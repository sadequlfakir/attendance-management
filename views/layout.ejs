<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Attendance System</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <aside class="w-64 bg-white shadow-lg p-6 hidden md:block">
        <h2 class="text-xl font-bold mb-6">🏢 Admin Panel</h2>
        <nav class="space-y-4">
          <a href="/users" class="block px-2 py-1 rounded hover:bg-gray-100"
            >👥 Users</a
          >
          <a href="/users/new" class="block px-2 py-1 rounded hover:bg-gray-100"
            >➕ Add User</a
          >
          <a
            href="/attendance/<%= new Date().toISOString().slice(0, 10) %>"
            class="block px-2 py-1 rounded hover:bg-gray-100"
            >📅 Today’s Attendance</a
          >
        </nav>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 p-6">
        <h1 class="text-2xl font-bold mb-6">📋 Office Attendance System</h1>
        <%- body %>
      </main>
    </div>
  </body>
</html>
